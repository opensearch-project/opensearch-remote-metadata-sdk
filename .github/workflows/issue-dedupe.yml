---
name: Issue Dedupe Main
on:
  issues:
    types: [opened]
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
    inputs:
      issue_number:
        description: 'Issue number to check for duplicates'
        required: true
        type: string

jobs:
  detect-issue:
    if: >-
      (github.event_name == 'workflow_dispatch' &&
       github.repository == 'opensearch-project/opensearch-remote-metadata-sdk') ||
      (github.event_name == 'issues' &&
       github.event.issue.user.type != 'Bot' &&
       github.repository == 'opensearch-project/opensearch-remote-metadata-sdk')
    uses: opensearch-project/opensearch-build/.github/workflows/issue-dedupe-detect.yml@27f1ebc46c1ca3f4823848ad7ba5622e55dfa562 # main
    permissions:
      contents: read
      issues: write
      id-token: write
    secrets:
      BEDROCK_ACCESS_ROLE_ISSUE_DEDUPE: ${{ secrets.BEDROCK_ACCESS_ROLE_ISSUE_DEDUPE }}
    with:
      issue_number: ${{ inputs.issue_number || '' }}
      grace_days: ${{ vars.DUPLICATE_GRACE_DAYS || '7' }}

  auto-close-issue:
    if: github.event_name == 'schedule' && github.repository == 'opensearch-project/opensearch-remote-metadata-sdk'
    uses: opensearch-project/opensearch-build/.github/workflows/issue-dedupe-autoclose.yml@27f1ebc46c1ca3f4823848ad7ba5622e55dfa562 # main
    permissions:
      issues: write
    with:
      grace_days: ${{ vars.DUPLICATE_GRACE_DAYS || '7' }}
